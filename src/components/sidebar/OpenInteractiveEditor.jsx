import React from "react";
import { v4 as uuidv4 } from "uuid";

export default function OpenInteractiveEditor({  }) {
    return (
    <div className="flex flex-col gap-2">
      
        Log Moves
      </button>
    </div>
  );
}
